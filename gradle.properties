# gradle.properties (루트에 위치)

# JVM 메모리 설정 (도커나 CI에서 OOM 방지)
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8

# 빌드 캐시 및 병렬 빌드 설정 (선택)
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.configureondemand=true